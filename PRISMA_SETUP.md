# Настройка Prisma с Neon

## ✅ Что уже сделано:

1. ✅ Создан `src/lib/prisma.ts` - PrismaClient
2. ✅ Обновлен `src/lib/users-storage.ts` - работает с Prisma
3. ✅ Обновлен `src/app/api/users/route.ts` - async функции
4. ✅ Исправлена схема Prisma (убраны url из datasource, исправлен enum Condition)

## 📋 Следующие шаги:

### 1. Добавьте DATABASE_URL в .env.local

Получите connection string из Neon dashboard и добавьте:

```env
DATABASE_URL="postgresql://user:password@ep-delicate-rain-ag635hj3-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require"
NEXTAUTH_SECRET=your-secret-key
NEXTAUTH_URL=http://localhost:3000
```

### 2. Создайте миграции

```bash
npx prisma migrate dev --name init
```

Это создаст таблицы в базе данных Neon.

### 3. Сгенерируйте Prisma Client

```bash
npx prisma generate
```

### 4. (Опционально) Просмотрите данные в Prisma Studio

```bash
npx prisma studio
```

## ⚠️ Важно:

- После миграций все данные будут храниться в базе данных Neon
- Файловое хранилище (`users-data.json`) больше не используется
- Убедитесь, что DATABASE_URL правильный (из Neon dashboard)
- При первом запуске база будет пустой - нужно будет зарегистрировать пользователей заново

## 🔄 Миграция существующих данных (если нужно):

Если у вас есть данные в `users-data.json`, их нужно будет мигрировать вручную или создать скрипт миграции.
